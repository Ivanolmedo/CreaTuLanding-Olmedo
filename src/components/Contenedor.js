import React from 'react';

function Contenedor({ mensaje }) {
  return (
    <div className="contenedor">
      <h2>{mensaje}</h2>
      {/* Productos acá */}
    </div>
  );
}

export default Contenedor;
